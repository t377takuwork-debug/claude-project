# オールスター感謝祭 タイムテーブル記事リライト

「オールスター感謝祭」（TBS系クイズ特番）を最新情報にリライトし、`draft_allstar.txt` に差分更新（Edit）する。

**2026-09-26新設。運用実績は立ち上げの1回のみ**。手順で迷ったら `rewrite_common_rules.md` を最優先で確認し、判断に迷う箇所はユーザーに確認してから進める。

## 番組の性質（重要）

- **年2回の生放送特番（春＝3〜4月・秋＝10月ごろ）**。音楽番組ではなくクイズ特番のため、「出演順」ではなく**企画の流れ**を軸にする（2026-09-26ユーザー指定）
- 放送枠は回で変わる。2025秋 18:30〜23:48／2026春 18:30〜23:50／2026秋 17:40〜23:50（開始が約50分早い）。「前回と同じ枠」を前提にしない
- 分単位の公式タイムテーブルは公開されない。過去回の時間帯は、報道・TVでた蔵・公式Xの投稿時刻から組んだ**目安**であり、記事でも「目安」と明記する
- 前後に関連番組が付く（前夜『超タッグ祭』・深夜『オールスター後夜祭』）。回ごとに有無が変わる（2026春の後夜祭は休止）
- 公式：https://www.tbs.co.jp/kanshasai/ ／公式X @allstar_tbs（TBS公式サイトはWebFetchがブロックされるため、確認はブラウザ画面で行う）
- 記事URL：https://shira-treat.com/allstar-kanshasai-timetable/
- 見逃し配信：TVer（https://tver.jp/series/srv8sdsc13）またはTBS FREE（https://cu.tbs.co.jp/program/28647）で期間限定配信の模様（2026-09-26ユーザー提供。配信期間は未確認）
- リアルタイム配信：https://tver.jp/live/tbs（TVerのTBS系リアルタイム配信）
- 「見逃し配信」H2に配信ガイドaside（`aria-label="オールスター感謝祭 配信ガイド"`）あり。公式サイトの3列構成（リアルタイム／最新回を無料配信／これまでの配信）に合わせ、3列目にU-NEXTのA8アフィリエイトリンク（`rel="nofollow"`のみ・計測ピクセル`<img>`をリンク直後に配置・「PR」表記付き）を置く。リンクURLの`&`は`&amp;`で書く
- **画像の使い分け（2026-09-26ユーザー指定）**：アイキャッチ画像（`image-10-2.webp`）は**構造化データ（`image`／`thumbnailUrl`）にだけ**使い、記事内には置かない。記事内の画像は `image-9-1.webp`（出演者一覧H2の`wp:image`）
- 配色：濃紺（`#0a0b22`）×金（`#f2c230`）×赤（`#e3262a`）。公式ロゴに寄せた配色のため、他番組の配色に寄せない
- BreadcrumbListは**カテゴリ階層を入れていない2階層**（ホーム→記事）。WP側のカテゴリURLが確定したら3階層へ直す

## 実行フロー判定

| 状況 | 実行する手順 |
|---|---|
| **出演者の追加発表**（資料①あり） | 3・4・6・7・9・JSON-LD |
| **企画の順番・時間帯が発表された** | 5に加えて、「企画の順番の傾向」欄の注記を更新 |
| **放送終了後**（実際の流れの資料②あり） | 「実際の流れ【放送後更新】」欄を今回の流れへ差し替え、旧「直前回」を「過去のタイムテーブル一覧」へ移す |
| **資料が渡されなかった** | 先にブラウザ画面で公式サイト、WebSearchで報道（TV LIFE・モデルプレス・オリコン等）を確認し、ユーザーに承認を得てから進める |

## Grepフェーズ（作業開始時に1メッセージで並行実行）

| 対象 | Grep文字列 |
|---|---|
| タイトル・メタ | `タイトル：オールスター感謝祭` |
| 速報ガイドaside | `aria-label="オールスター感謝祭 速報ガイド"` |
| 締め文 | `\[nopc\]\[mokujimae\]\[/nopc\]` |
| H2最新タイムテーブル | `最新タイムテーブル【` |
| タイムテーブルaside | `aria-label="オールスター感謝祭 タイムテーブル（` |
| H2出演者一覧 | `出演者一覧【` |
| 出演者aside | `aria-label="オールスター感謝祭 出演者一覧"` |
| H2何時から・関連番組 | `aria-label="オールスター感謝祭 放送スケジュールと関連番組"` |
| 時間帯ごとの予想aside（H3×7：17：40～〜23：00～） | `aria-label="オールスター感謝祭 タイムテーブル予想（` |
| 企画の順番の見方aside | `aria-label="オールスター感謝祭 企画の順番の見方"` |
| 放送アーカイブaside（直前回） | `aria-label="オールスター感謝祭 放送アーカイブ"` |
| 過去一覧aside | `aria-label="オールスター感謝祭 過去のタイムテーブル（` |
| FAQ aside | `aria-label="オールスター感謝祭 よくある質問"` |
| ナビブロック | `他の音楽番組もチェック` |
| JSON-LD | `MANUAL_JSONLD` |

## 更新手順の要点

1. **タイトル**：`オールスター感謝祭2026秋のタイムテーブル｜何時から？出演者一覧・最新情報`（数値は入れない。回が変わったら「2026秋」の部分だけ差し替え、KW「タイムテーブル」「何時から」「出演者」は落とさない）。メタは「出演順」か「順番」を必ず含める
2. **速報ガイドaside**：SCHEDULE・MC・発表状況（企画の順番が未発表か発表済みかを1行で。発表されたら「◯時台まで発表」等へ更新）・出演者（代表4名＋「他、総勢◯名以上」。『VIVANT』の出演者は赤の「VIVANT」バッジ付きで目立たせる）・SPECIAL・Update日付。ラベルは英字の「SCHEDULE」と日本語の「発表状況」「出演者」が混在するが、2026-09-26のユーザー指定による
3. **最新タイムテーブル**：放送枠asideの下に、時間帯ごとのH3（17：40～／18：00～…23：00～）と「予想」カードを置く（2026-09-26確定）。未発表の間は、H3見出しにも「（予想）」、カードにも「予想」バッジと「確度」を付け、根拠は「過去◯回のうち◯回」の形で書く（`rewrite_common_rules.md` 10-3）。**企画の順番が公式発表されたら、H3見出しの「（予想）」とバッジを外し、内容を確定情報へ書き換える。放送後は「実際の流れ」欄へ移す。予想が外れた場合は消さずに残す**。予想の根拠は過去3回（2025春=今回と同じ17:40スタート／2025秋／2026春）。回が増えたら根拠に加える
4. **出演者一覧**：作品別カードへ追記。誰がどの企画に出るかは、資料に書かれた範囲のみ
5. **企画の順番の傾向**：3回分以上の実データが溜まったら、断定調（「〜が続いています」）へ切り替える。それまでは「過去◯回の放送では〜」の事実ベース（`rewrite_common_rules.md` 10-3）
6. **FAQ**：Q1（放送時間）・Q2（順番の事前発表）・Q3（出演者）・Q4（ミニマラソンの時刻）・Q5（見逃し配信・固定文言）。本文とJSON-LD FAQPageは一字一句一致させる
7. **過去一覧**：直前回は「実際の流れ」欄にのみ置き、過去一覧には前々回以前だけを入れる（`rewrite_common_rules.md` 11-1）。現在のH3は新しい順に2025秋・2025春の2つ（2025春＝3/29は今回と同じ17:40スタート）

## JSON-LD

- 毎回更新：`description`（メタと一字一句同一）・`dateModified`・`keywords`・BroadcastEvent（`name`／`startDate`／`endDate`／`performer`）・ItemList（`numberOfItems`と一覧）・FAQPage
- 変更時のみ：`headline`（タイトル変更時）・`image`／`thumbnailUrl`（新アイキャッチ）
- ItemListにはMC2名と出演者を並べている（performerと同じ並び）。人数はタイトルに入れない

## 保存・チェックリスト

保存先：`C:\Users\PC_User\claude project\blogs\shira_note\drafts\draft_allstar.txt`（Edit差分更新）

最終チェック（ERROR 0件・新規WARN 0件で完了）：

```
python tools/qa_draft.py draft_allstar.txt --fix
```

`qa_draft.py` はこの番組のFAQ（`Q1｜`形式のdiv）の本文⇔JSON-LD同期を自動判定できない。**完了前に本文とJSON-LDのQ&A一致を手作業で確認する**（Q文は「Q{n}｜」を除いた文言が`name`と一致）。

## 未対応事項（2026-09-26時点）

- `tools/qa_draft.py` の `NAV_FIXED_URLS`・`NAV_SELF_URL_BY_FILENAME`・`CANONICAL_URL_BY_FILENAME` と `rewrite_common_rules.md` 8章のナビURL表には、この番組を**まだ登録していない**。登録すると他の全draftのナビブロックにこの番組の行を足す作業が同時に発生するため、公開後にユーザーと相談して行う
- 他の全番組draftのナビブロックへの、この番組の行の追加
- WPカテゴリURLの確定（確定したらBreadcrumbListを3階層へ）
- 出演者・企画の順番・配信サービスの発表内容の反映
