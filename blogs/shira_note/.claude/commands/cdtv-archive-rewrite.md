# CDTVライブ！ライブ！ 月次アーカイブ記事リライト

CDTVライブ！ライブ！の月次アーカイブ記事を指定月の内容にリライトし、`draft_cdtv_archive.txt` として上書き保存する。

## 事前情報の確認

リライト開始前に以下の情報が揃っているか確認する。不足している場合はユーザーに確認する。

### 必須情報（資料①）
- 対象月（YYYY年MM月）
- 当月の放送回数と各放送日（例：5/11、5/18）
- 各放送回ごとの情報：
  - 出演アーティスト一覧（出演順）
  - 各アーティストの歌唱曲
  - 特記事項（マンスリーライブ・特設ブロック・コラボ・初出演・フルサイズ歌唱・特別演出など）
  - 推定タイムライン（時刻・アーティスト・曲名）
- 記事URL（PAGE_URL：`https://shira-treat.com/〇〇/`）
- 記事の初回公開日（DATE_PUBLISHED）

### 任意情報
- アイキャッチ画像URL（変更がある場合のみ）

## 参照ファイル

| ファイル | 用途 | 読み込みタイミング |
|---|---|---|
| `template_jsonld_cdtv_archive.txt` | JSON-LD 骨格テンプレート | 手順1で必ず Read |
| `template_cdtv_archive_static.txt` | 固定ブロック（H2・WPコメント込み・完全コピー用） | 手順1で必ず Read |
| `C:\Users\PC_User\claude project\blogs\seo\SEO_guide.txt` | SEOチェックリスト | 任意（SEO判断に迷った場合のみ） |

## 作業方針

| 原則 | 内容 |
|---|---|
| **ショートコードは独立ブロック** | `[nopc][title]`・`[nopc][mokujimae]`・`[nopc][originalsc]` はそれぞれ独立した `<!-- wp:paragraph -->` ブロックに分割する |
| **月次リビルドはWrite可** | アーカイブ記事は毎月新規内容で全体再構築するため、Write（全体上書き）を使用してよい |
| **テンプレートは変更なしブロックに使用** | `template_cdtv_archive_static.txt` の3ブロックはそのままコピー |
| **ユーザー修正後の確認** | 修正後はsystem-reminderの差分を優先参照し、JSON-LD整合性など必要箇所のみRead・確認する |

---

## WordPressブロックコメント規則（必須）

ドラフトファイル全体を通じて、以下のブロックコメントを必ず使用する。

| コンテンツ種別 | 形式 |
|---|---|
| テキスト段落 | `<!-- wp:paragraph --><p>本文</p><!-- /wp:paragraph -->` |
| H2見出し | `<!-- wp:heading --><h2>見出し</h2><!-- /wp:heading -->` |
| H3見出し | `<!-- wp:heading {"level":3} --><h3>見出し</h3><!-- /wp:heading -->` |
| HTML全ブロック（aside・table・div等） | `<!-- wp:html -->...<!-- /wp:html -->` |
| 画像 | `<!-- wp:image {"align":"center"} -->...<!-- /wp:image -->` |
| JSON-LD | `<!-- wp:html --><!-- MANUAL_JSONLD_ARCHIVE --><script>...</script><!-- /wp:html -->` |
| **ショートコード（独立必須）** | `<!-- wp:paragraph --><p>[nopc][title][/nopc]</p><!-- /wp:paragraph -->` |

**モデルファイル**: `draft_cdtv_archive.txt` が正規の出力形式。形式に迷った場合はこのファイルを参照する。

---

## リライト手順

### 1. ファイル準備

以下の順序でファイルを読み込む。

**① テンプレート2点を Read する**
- `template_jsonld_cdtv_archive.txt`：JSON-LD のプレースホルダー構造を確認
- `template_cdtv_archive_static.txt`：固定ブロック3点（傾向・見逃し配信・関連リンク）を取得

**② 既存の `draft_cdtv_archive.txt` を Read して内容全体を確認する**
- 直前月のドラフトがベース。放送前の回がある場合はカードグリッド形式の実装例として参照する。
- 月次リビルドのため全体Readは許容。ただし `MANUAL_JSONLD_ARCHIVE` をGrepで確認し、JSON-LD行番号を把握しておく。

**③ 更新完了後、`draft_cdtv_archive.txt` を Write で上書き保存する**
`C:\Users\PC_User\claude project\blogs\shira_note\draft_cdtv_archive.txt`

---

### 2. タイトル・メタディスクリプション

**タイトル**
```
【CDTV】YYYY年M月の出演者や曲目・セトリ一覧まとめ（放送アーカイブ）
```
- `【CDTV】` を冒頭に固定
- 年・月を正確に記載（例：2026年5月）
- `（放送アーカイブ）` を末尾に固定

**メタディスクリプション（120字以内）**

ドラフトファイルのタイトル行の直下に以下の形式で記載する。

```
メタディスクリプション（XXX字）：
YYYY年M月放送「CDTVライブ！ライブ！」のタイムテーブル、出演者、歌唱曲などを過去のアーカイブとして掲載。振り返り用として{月内の最大見どころ1〜2点}もまとめています。
```

#### 作成ルール

| 項目 | 内容 |
|---|---|
| 冒頭 | 「YYYY年M月放送「CDTVライブ！ライブ！」のタイムテーブル、出演者、歌唱曲などを過去のアーカイブとして掲載。」で固定 |
| 見どころ | マンスリーライブ出演者・特設ブロック・注目コラボなど、その月固有の見どころを簡潔に |
| 末尾 | 「まとめています。」で固定 |
| 文字数 | 100〜115字を目安。120字を超えない |

---

### 3. 冒頭段落・asideカード

**冒頭段落（1段落のみ・月名のみ更新）**

```
<!-- wp:paragraph -->
<p>この記事では、YYYY年M月に放送された『CDTVライブ！ライブ！』の出演アーティストや歌唱曲、そしてタイムテーブル（出演順）をアーカイブとしてまとめています。[nopc][title][/nopc]</p>
<!-- /wp:paragraph -->
```

**asideカードの更新箇所**

| プレースホルダー | 更新内容 |
|---|---|
| BROADCAST 表示 | 「YYYY年M月度」のみ（放送時間の記載は不要） |
| STATUS | 当月の全放送回セットリスト確定状況（例：「M/DD・M/DD 放送回セットリスト確定」） |
| 最新速報リンク | `href="https://shira-treat.com/cdtv-timetable/"` は固定（変更不要） |
| LINEUPバッジ | 各放送日ごとに「M/DD LINEUP」バッジを作成し、出演者を `/` 区切りで列挙（放送日が多い場合はバッジを追加） |
| 注意書き（黄色枠） | 「公式タイムテーブル非公表のため、SNSのリアルタイム投稿・熱量を基準に実況順を再現しています」は固定 |
| ボタンテキスト | 「M月放送のタイムテーブル・セトリ一覧を見る ▶」に更新 |

**締め文（`[nopc][mokujimae][/nopc]` 直後）**

当月の特徴（放送回数・マンスリーライブアーティスト・特設ブロックなど）を1文で紹介する形に書き換える。原則は以下の固定文を維持しつつ、今月固有の要素を加える。

```
当サイト独自の視点として、反響を交えた見どころ解説も掲載しているので、放送を振り返る際の参考にぜひ活用してください！
```

---

### 4. H2：YYYY年M月のCDTVライブ！ライブ！放送スケジュール

H2タイトルの年・月を更新する。

アイキャッチ画像（`<figure>`タグ）の `src` は変更不要（同じ画像を使用）。ただし指定がある場合は差し替える。

**放送スケジュール表の各行を当月の放送日で更新する。**

| 要素 | 更新内容 |
|---|---|
| 日付（MAY/JUN など月略称） | 当月の英語略称に更新 |
| 放送日・曜日 | 正確な日付・曜日に更新 |
| サブテキスト | 主な出演者と番組内容の特徴を記載 |
| ステータスバッジ | 放送済は `放送済`（グレー）。次回予告回は `最新速報`（黄色）でリンク付き |

**放送回数が2回以外の場合の対応**
- 3回以上の場合：`<div>` ブロックを追加して行を増やす
- 最終行（次回予告）は常にリンク付きの「最新速報」形式で残す

締め文は当月の放送全体の印象を1〜2文でまとめる。

---

### 5. H2：M月DD日（曜日）のタイムテーブル・セットリスト（新しい日付から降順）

**放送日ごとにH2セクションを1つ作成する。新しい放送日が上に来るよう降順で並べる。**

#### 5-a. 歌唱曲一覧（放送状況で形式を使い分ける）

**【放送済み回】テーブル形式**

| カード要素 | 更新内容 |
|---|---|
| ヘッダー | 「M月DD日 出演アーティスト・歌唱曲一覧」 |
| ARTIST列 | 出演者名（出演順に並べる） |
| SONG列 | 歌唱曲名。複数曲は ` ／ ` 区切り |
| バッジ（ピンク） | フルサイズ・初披露・TV初披露など特記事項 |
| バッジ（黄色） | マンスリーライブ週数・特設ブロック |
| バッジ（グレー） | 衣装・演出などの補足 |

テーブルの行は偶数・奇数で背景色を交互にする（`#000` / `#050505`）。

**【放送前・事前発表のみの回】カードグリッド形式**

2カラムカードグリッド（通常アーティスト）＋特設ハイライトカード（マンスリーライブ・周年企画・特別フェス等）を使用する。

- **通常カード**: `background:#111; border:1px solid #222;` + 上部グラデーションライン（`#ff008d → #ff00ea`）。アーティスト名（白・太字イタリック）と楽曲名（黄色・10px）を記載
- **ハイライトカード**: `border:1px solid #ff008d;`（マンスリー/周年）または `border:1px solid #222;` + 黄色グラデーション（特別フェス）。バッジ・楽曲一覧・説明文付き
- 出演順未確定の場合は「出演順は放送後確定」と注記し、50音順またはセグメント別に整理する

`draft_cdtv_archive.txt` の最新月の放送前セクションが実装例。

#### 5-b. タイムライン（出演順アーカイブ）

タイトルバーの `M月DD日 SETLIST TIMELINE` を更新する。

| タイムライン要素 | 更新内容 |
|---|---|
| 時刻バッジ | 「HH:MM頃〜」（19時台は白背景・黒文字、20時台は黄色背景・黒文字） |
| アーティスト名 | 通常は白文字。特設ブロック・マンスリー枠はピンク文字 |
| 曲名 | グレー文字で小さく表示。複数曲は改行 |
| 特設ブロック | ピンク時刻バッジ・ピンクアーティスト名・詳細説明付きで強調 |

タイムラインの直後に、その放送回の見どころ・SNS反響・特筆すべきパフォーマンスを2〜4文で記載する（`[nopc][originalsc][/nopc]` は使用しない）。

---

### 6. 傾向・見逃し配信・まとめ（固定ブロック）

`template_cdtv_archive_static.txt` から以下の3ブロックをそのまま順番にコピーする。H2・WPブロックコメント・導入文・締め文すべて含まれているため変換不要。

| ブロック | 唯一の変更箇所 |
|---|---|
| BLOCK: 出演順・タイムテーブルの傾向 | なし（完全固定） |
| BLOCK: 見逃し配信・再放送 | なし（完全固定） |
| BLOCK: まとめ導入文＋関連番組リンク | 1文目の `YYYY年M月` を対象月に差し替えるだけ |

---

### 7. 構造化データ（JSON-LD）の更新

**`template_jsonld_cdtv_archive.txt` を骨格として使用し、ALL_CAPS プレースホルダーを差し替える。**

#### 差し替え箇所

| プレースホルダー | 差し替え内容 |
|---|---|
| `PAGE_URL` | 記事URL（末尾スラッシュ付き） |
| `HEADLINE` | タイトル（WordPressタイトル欄と同じ文字列） |
| `META_DESCRIPTION` | メタディスクリプションと同じ文字列 |
| `DATE_PUBLISHED` | 記事初回公開日時（ISO形式：YYYY-MM-DDTHH:MM:SS+09:00） |
| `DATE_MODIFIED` | 作成・更新日時（ISO形式：YYYY-MM-DDTHH:MM:SS+09:00） |
| `EYECATCH_URL` | アイキャッチ画像URL |
| `MONTH_JA` | 対象月（例：2026年5月） |
| `ARTIST_KEYWORDS` | 月内全出演者名をカンマ区切りで列挙（`"名前", "名前"` 形式） |
| `N_BROADCASTS` | 当月の放送回数（整数） |
| `BREADCRUMB_NAME` | パンくずリスト最終階層テキスト（例：2026年5月アーカイブ） |
| `BROADCAST_EVENT_LIST` | 各放送日のListItem（ひな形を放送回数分展開、新しい日がposition:1） |

#### ItemListの各BroadcastEventに記載する内容

| 項目 | 内容 |
|---|---|
| `name` | 「CDTVライブ！ライブ！（YYYY年MM月DD日放送）」 |
| `startDate` | 放送開始ISO形式（通常：`YYYY-MM-DDT19:00:00+09:00`） |
| `endDate` | 放送終了ISO形式（通常枠：`YYYY-MM-DDT21:00:00+09:00`、3時間SP：`22:00` など） |
| `performer` | 当回出演者をMusicGroup/Person型で列挙 |

#### アーティストの型判定

| 区分 | 型 |
|---|---|
| グループ・バンド | `{ "@type": "MusicGroup", "name": "アーティスト名" }` |
| ソロ（個人名） | `{ "@type": "Person", "name": "アーティスト名" }` |

#### 固定箇所（変更不要）

- `author` / `publisher`
- `about`（CDTVの番組情報）
- `BreadcrumbList` の1・2階層目

---

## 保存・確認

- 保存先：`C:\Users\PC_User\claude project\blogs\shira_note\draft_cdtv_archive.txt`（上書き）
- 構造化データは末尾に追記（WordPressのカスタムHTML欄に貼り付けて使用）

### SEOチェックリスト（保存後に実施）

`C:\Users\PC_User\claude project\blogs\seo\SEO_guide.txt` を参照しつつ、以下を確認する。

| フェーズ | 確認ポイント |
|---|---|
| タイトル | 「【CDTV】」が冒頭にあるか／年・月が入っているか／「（放送アーカイブ）」が末尾にあるか |
| メタディスクリプション | 年月・見どころ・アーカイブ訴求が含まれているか／120字以内か |
| asideカード | 全放送日のLINEUPバッジが揃っているか／STATUSが最新か |
| 放送スケジュール | 全放送日が正しく記載されているか／ステータスバッジが正確か |
| セットリスト | 各放送日の出演者・曲名・特記バッジが漏れなく記載されているか |
| タイムライン | 時刻・アーティスト・曲名が出演順通りか／特設ブロックが強調されているか |
| まとめ | 月名が正しく更新されているか |
| JSON-LD | 全プレースホルダーが差し替えられているか／放送回数分のBroadcastEventが揃っているか |
| ページURL | `PAGE_URL` プレースホルダーが実際のURLに置き換わっているか |
