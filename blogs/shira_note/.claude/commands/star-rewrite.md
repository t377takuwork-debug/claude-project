# STAR タイムテーブル記事リライト

フジテレビ音楽番組『STAR』のタイムテーブル記事を最新情報にリライトし、`draft_star.txt` として上書き保存する。

## 事前確認

リライト開始前に以下が揃っているか確認。不足時はユーザーに問い合わせる。

| 区分 | 内容 |
|---|---|
| 資料①（必須） | 放送日・出演者・披露曲・見どころ・ナビゲーター（なし回は「なし」と明記） |
| 資料②（推奨） | 直前回のX投稿分析：出演順・時間帯・SNS反応・TVerエピソードURL |
| その他 | アイキャッチ画像URL |

資料①だけでもリライト可。資料②がない場合はアーカイブパネルの出演順を前ドラフトの予測情報ベースで記載し、その旨を補足メモに残す。

**ナビゲーターなし回・ゲスト出演者の扱い**

| ケース | 処理 |
|---|---|
| ナビゲーター設定なし | aside②の19:00行を「OP（MC上垣皓太朗）」のみにする。アーカイブパネルのSTAR NAVIGATORS行は「上垣 皓太朗（フジテレビアナウンサー）」のみ記載 |
| 歌唱なしゲスト（例：俳優・タレント） | aside①「出演：」欄・アーティストラインナップ一覧には含めない。aside②の19:00 OPに「ゲスト：〇〇」として記載。JSON-LD performerには `Person` として含める |

## 番組基本情報（固定）

| 項目 | 内容 |
|---|---|
| 放送 | フジテレビ系 木曜 19:00〜20:00 |
| MC | 上垣皓太朗（フジテレビアナウンサー） |
| ナビゲーター | 毎回変わる |
| 公式サイト | https://www.fujitv.co.jp/star/ |
| TVerシリーズURL | https://tver.jp/series/sr1ksvsv1n（固定） |
| 記事URL | https://shira-treat.com/star-timetable/（固定） |

## 参照ファイル（必要時のみ読む）

- `persona_timetable.txt`：ペルソナ（佐藤 真由）
- `C:\Users\PC_User\claude project\blogs\seo\SEO_guide.txt`：SEO設計
- `template_star_static.txt`：見逃し配信・ナビゲーションブロックのバックアップ（復元・確認時のみ読む）

---

## 作業方針（必読）

### Read・Edit・スキップの原則

| 原則 | 内容 |
|---|---|
| **全体Readしない** | ファイルは1000行超。Grepで対象セクションの行番号を特定してからoffset/limitで必要範囲だけ読む |
| **WriteしないでEditする** | 変更箇所のみEditで差分更新。Write（全体上書き）は使用しない |
| **静的セクションはスキップ** | 下記の静的セクションはRead・Edit不要 |

### 静的セクション（Read・Edit完全スキップ）

- 冒頭2段落（`STAR（フジテレビ新音楽番組）〜` と `公式発表まで〜`）
- H2「STARは見逃し配信・再放送ある？」とその内容全体
- ナビゲーション・アフィリエイトブロック
- JSON-LDの固定フィールド（`@id` / `url` / `datePublished` / `dateCreated` / `author` / `publisher` / `about` / `BreadcrumbList`）

### セクション特定用Grepマーカー

各セクションの読み込み前にGrepで行番号を確認し、offset/limitを決める。

| 対象セクション | Grep検索文字列 |
|---|---|
| タイトル・メタ | `タイトル：【最新】STAR` |
| aside①（番組情報まとめ） | `aria-label="番組情報まとめ"` |
| aside②（タイムテーブル速報） | `aria-label="STARタイムテーブル速報"` |
| アーティストラインナップ | `aria-label="出演者＆歌唱曲一覧"` |
| 出演時間の目安 | `aria-label="出演時間の目安"` |
| アーカイブパネル | `aria-label="STAR放送アーカイブ"` |
| 過去タイムテーブル | `STAR 過去タイムテーブル（アーカイブ）` |
| FAQ | `aria-label="STAR FAQ"` |
| JSON-LD | `MANUAL_JSONLD` |

### ユーザー修正後の確認

ユーザーが修正した場合、**system-reminderに差分が表示される**。全体Readは不要。差分から変更箇所を把握し、JSON-LD FAQ整合性など必要な追加確認のみ行う。

---

## 作業手順

### 1. タイトル・メタディスクリプション

**Read**: offset 0, limit 7（Grepは不要。ファイル先頭の固定位置）

**ファイル先頭の構造（WPブロック囲み）**:
```
<!-- wp:paragraph -->
タイトル：【最新】STAR タイムテーブル ...
（空行）
メタディスクリプション（XXX字）：
...メタテキスト...
<!-- /wp:paragraph -->
```

**Edit対象**: `<!-- wp:paragraph -->` 〜 `<!-- /wp:paragraph -->` のブロック全体を1回のEditで差し替える。ブロックタグを含めてold_stringを組むこと（タグなしで2行だけEditするとWP形式が崩れる）。

**タイトル形式（固定）**
```
【最新】STAR タイムテーブル M月DD日は何時から？出演順や出演時間更新！
```

**メタディスクリプション（100〜115字・120字以内）**
```
M/DD放送のフジテレビ音楽番組『STAR』出演アーティスト・歌唱曲・タイムテーブル速報。{今回固有の注目ポイント}など随時更新！
```
- アーティスト名＋「初コラボ」「初披露」などの希少性ワードを含める
- 前回と同じ表現を使わない
- **メタの確定文字列は手順15のJSON-LD descriptionに丸ごとコピーして使う**

---

### 2. 冒頭段落（スキップ）

**Read・Edit不要。** 内容固定のため触らない。

---

### 3. aside①「番組情報まとめ」カード

**Read**: `aria-label="番組情報まとめ"` をGrepで特定 → 前後100行を読む

**Edit対象**: 以下の6箇所を個別にEdit

| 要素 | Edit対象の特定文字列 | 更新内容 |
|---|---|---|
| 放送日時 | 前回の日付（例：`6.11 <span`） | `M.DD (木) 19:00 - 20:00` |
| UPDATED | 前回の`UPDATED: 20YY.MM.DD` | 作成日（YYYY.MM.DD） |
| 出演：行 | 前回の出演者列 | `/` 区切り＋`and more...` |
| 注目：行 | 前回の注目テキスト | 今回固有の見どころ2〜3項目 |
| タイムテーブル：行 | 前回のステータス文 | 放送前は「現在は番組構成予測を掲載中。放送開始後（19:00〜）に実際の進行へ随時更新します。」／放送後に再訪した場合は実際の進行に即した文言へ調整 |
| ※ハイライト | 前回のハイライト文 | 1文のキャッチコピー（汎用表現禁止） |

**リンクボタン（「最新タイムテーブルを見る ❯」）**: `href="#st-toc-h-1"` の記事内アンカー固定（H2「STAR 最新タイムテーブル」への直接ジャンプ）。`target="_blank"`は付けない。他番組（mste/fns/musicday/ongakunohi/teretou/utadeaetara/dancebattle）と同じ`#st-toc-h-N`方式に統一済み（2026-07-11）。**外部の公式サイトURLに戻さないこと**（旧仕様の名残でfujitv.co.jpへのリンクに戻す誤修正が過去に発生）。

---

### 4. aside②「タイムテーブル速報」カード

**Read**: `aria-label="STARタイムテーブル速報"` をGrepで特定 → 前後120行を読む

**Edit対象**: 放送日時・UPDATED・全時間帯ブロック・※最新の状況

| 要素 | 更新内容 |
|---|---|
| 放送日時 | ③と同じ |
| UPDATED | 作成日 |
| 各時間帯ブロック | 今回の出演者・企画に合わせて書き換え（19:00〜19:58頃、ブロック数は出演者数に応じて増減） |
| ※最新の状況 | 現在の記事状態＋今回の注目点（1〜2文） |

時間帯ブロックは前回の全ブロック（`✦`で始まるdivの塊）を一括でEditする。

---

### 5. 締め文（`[nopc][mokujimae][/nopc]` 直後の段落）

**Read**: aside②のRead範囲に含まれるため追加Readなし

**Edit対象**: `[nopc][mokujimae][/nopc]` 直後の段落（1つまたは2つの `<p>`）

「推しの出演順は？」「何時に歌う？」の定型句を維持し、今回固有の要素を補足する。1段落にまとめるか2段落に分けるかはコンテンツ量に応じて判断。

---

### 6. H2：STAR 最新タイムテーブル【M/DD】

**Read**: 追加Readなし（手順4の範囲に含まれる）

**Edit対象**:
- H2テキスト（`<h2>STAR 最新タイムテーブル【前回日付】</h2>`）
- リード文の日付（`2026年MM月DD日（木）`）

---

### 7. H2：STAR（M/DD）出演アーティスト・歌唱内容一覧

**Read**: `aria-label="出演者＆歌唱曲一覧"` をGrepで特定 → 前後80行を読む

**Edit対象**:
- H2テキストの日付
- アーティストラインナップ全行（前回の全カード塊を一括Edit）
- リスト直後の本文（2〜3文）

背景色：通常行（白）と目立て行（`#fdf2ed`）を交互。歌唱曲未定の場合：「歌唱曲は後日発表」

---

### 8. H2：出演時間は何時頃？構成傾向【目安】

**Read**: `aria-label="出演時間の目安"` をGrepで特定 → 前後80行を読む

**Edit対象**: 3ブロックの説明文と💡まとめ（前回テキストを一括Edit）

| ブロック | 目安時間 |
|---|---|
| オープニング・熱狂枠 | 19:00〜19:15頃 |
| 中盤・パフォーマンス枠 | 19:15〜19:45頃 |
| 終盤・クライマックス枠 | 19:45〜20:00頃 |

「💡 今回のポイント」は直前回の実績データを根拠として今回の構成を予測する文にする。

---

### 9. H2：STAR の実際の出演順【放送後更新】

**Read**: `aria-label="STAR放送アーカイブ"` をGrepで特定 → 前後160行を読む

> ⚠️ **このReadの内容を退避データとして保持する**（手順11で使用）。ここに表示されているのは「前々回」のデータであり、これから資料②で上書きされて消える。手順11の過去一覧に追加するのはこの退避データであり、これから書き込む資料②のデータではない（2026-07-18 Mステの同種ミスを受けて全番組共通で明文化）。

**Edit対象**: アーカイブパネル（`<aside class="star-archive-panel"` 〜 `</aside>` の全体を一括Edit）

> ⚠️ このステップはHTML全体置換のためスマートクォート（`"`等）混入リスクが最も高い。Edit後、手順16の検証コマンドを必ず実行する。

| 要素 | 更新内容 |
|---|---|
| ヘッダー日付 | 直前回の放送日（例：`2026.06.04 THU`） |
| STAR NAVIGATORS行 | 直前回のMC・ナビゲーター名 |
| タイムライン各ブロック | 直前回の出演順・楽曲・コメント |
| REPORT文 | 直前回の放送全体のまとめ（文体ルール▼参照） |
| TVerリンク | 直前回のエピソードURL（不明の場合はシリーズURL） |
| FINAL UPDATED | 直前回の放送日 |

加えて:
- 冒頭リード文「MM月DD日の放送は〜」の日付を更新
- 末尾注記「※M/Dは放送前のため〜」の日付を今回放送日に更新

**REPORT文の文体ルール**:
- X情報を元にしていてもX由来であることは書かない（「X実況投稿に基づく」「投稿が爆発」「Xが沸いた」「トレンド入り」等は使用禁止）
- 反響は自然な文体で表現する：「〜が好評」「〜が盛り上がった」「〜が話題を集めた」「〜が最大のハイライトに」など
- 放送の流れ（出演順の大まかな流れ）＋反響のポイント1〜2点を2文程度にまとめる

---

### 10. H2：STARは見逃し配信・再放送ある？（スキップ）

**Read・Edit不要。** TVerシリーズURL固定のため内容変更なし。

---

### 11. H2：過去のタイムテーブル（STAR参考データ）

**Read**: `STAR 過去タイムテーブル（アーカイブ）` をGrepで特定 → 前後200行を読む

**Edit対象（2回のEdit）**:

1. **先頭追加**: `<div style="padding:14px 16px;">` 直後に、手順9で退避したデータ（アーカイブパネルの上書き前の内容）をH3＋カードブロックとして挿入（資料②のデータではない）
2. **末尾削除**: 追加後に5回分になった場合、最古カードのH3〜`</div></div>` を削除して**4回分を常に維持**

各カードの要素：日付・（TVerリンクがあれば）・出演者ごとの歌唱曲・コメント・💡まとめコメント

**完了後セルフチェック**：過去一覧の新規追加カードの放送日と、実際の出演順アーカイブパネル（手順9）の放送日が同じになっていないかGrepで確認する（同じ日付が2箇所に出ていたら退避データと資料②の取り違え）。

> **注意**: 過去カードのWATCH ON TVerリンクは、エピソードURLが確定している場合のみ記載する。不明の場合はリンク行ごと省略する。

---

### 12. H2：よくある質問（FAQ）

**Read**: `aria-label="STAR FAQ"` をGrepで特定 → 前後80行を読む

**Edit対象**: Q1〜Q4の質問文とA文（前回Q&A全体を一括Edit）

- Q1: 最注目アーティストの出演時間予測
- Q2: 今回固有の企画・コラボ・ナビゲーター内容
- Q3: 特別企画や初披露など今回固有の内容
- Q4: タイムテーブルのリアルタイム更新について（日付のみ更新する定型文）

Q文は口語的な疑問形。A文は結論→根拠の構造。

---

### 13. H2：まとめ

**Read**: FAQのRead範囲の後続に含まれることが多い。なければ末尾50行をRead

**Edit対象**: `<h2>まとめ</h2>` 直後の段落群

放送日・出演者・見どころを今回の情報で書き換える（ユーザーが後から調整しやすい骨格で書く）。

---

### 14. ナビゲーション・アフィリエイトブロック（スキップ）

**Read・Edit不要。** 内容は完全固定。

---

### 15. 構造化データ（JSON-LD）

**Read**: `MANUAL_JSONLD` をGrepで特定 → 後続160行を読む

**Edit対象（動的フィールドのみ）**:

| フィールド | 更新内容 |
|---|---|
| `headline` | タイトルと同じ文字列 |
| `description` | **手順1で確定したメタの文字列を丸ごとコピー**（要約・言い換え不可） |
| `dateModified` | 作成日時（ISO形式） |
| `keywords` | 出演者名を最新に |
| `image` / `thumbnailUrl` | 今回のアイキャッチ画像URL |
| BroadcastEvent `name` | 「STAR（YYYY年MM月DD日放送回）」 |
| BroadcastEvent `startDate` / `endDate` | 放送日時（ISO形式） |
| BroadcastEvent `performer` | MC（上垣皓太朗）＋ナビゲーター＋今回の出演者 |
| ItemList `name` / `itemListElement` | 今回の出演者 |
| FAQPage `mainEntity` | 記事本文Q&Aと**一字一句一致**（3問） |

上記を1回のEditでJSON-LDブロック全体を差し替える（動的フィールドが多いため個別Editより一括の方が効率的）。

#### 固定箇所（変更不要）

`@id` / `url` / `datePublished` / `dateCreated` / `author` / `publisher` / `about`

BreadcrumbListはposition 1・2は固定。position 3の `name`（例：「6月18日 STAR タイムテーブル」）は放送日に合わせて毎回更新する。

#### アーティスト型判定

- グループ・バンド → `"@type": "MusicGroup"`
- ソロ → `"@type": "Person"`

#### 注意

- `broadcaster` は `フジテレビ`（テレビ朝日ではない）
- `publisher.name` は `Shira`（`Shira Notes` ではない）
- FAQのJSON-LDは**手順12のFAQ本文を書いた後に**記事テキストからコピーして合わせる。ユーザーが本文を修正した場合も必ず再確認する。

---

### 16. スマートクォート確認・修正（必須）

全Editが完了したら以下のPowerShellを実行する。スマートクォート（U+201C/D・U+2018/9）はHTMLの属性値（`class="..."` `style="..."`等）を壊しデザインが表示されなくなる。Edit tool の new_string 経由で混入するため毎回除去する。

```powershell
$path = "C:\Users\PC_User\claude project\blogs\shira_note\drafts\draft_star.txt"
$content = [System.IO.File]::ReadAllText($path, [System.Text.Encoding]::UTF8)
$content = $content.Replace([char]0x201C, '"').Replace([char]0x201D, '"')
$content = $content.Replace([char]0x2018, "'").Replace([char]0x2019, "'")
[System.IO.File]::WriteAllText($path, $content, [System.Text.Encoding]::UTF8)
Write-Output "スマートクォート置換完了"
```

---

## 保存・SEOチェックリスト

保存先：`C:\Users\PC_User\claude project\blogs\shira_note\drafts\draft_star.txt`（Edit差分更新）

| 確認ポイント | 基準 |
|---|---|
| タイトル | 「【最新】STAR タイムテーブル」冒頭固定・放送日入り |
| メタディスクリプション | 100〜115字・放送日・希少性ワード・更新訴求あり |
| **JSON-LD description** | **メタと一字一句一致（コピー元はメタ確定後のテキスト）** |
| aside①②・締め文 | 今回固有の内容（汎用表現なし） |
| 時間帯予測 | 今回の出演者ベースの具体的予測 |
| FAQ | Q文：口語疑問形 / A文：結論→根拠 |
| アーカイブパネル | 直前回の日付・出演順・TVerリンクが正しいか |
| 過去タイムテーブル | 直前回追加済み・古い回削除済み |
| TVerURL | シリーズURL（固定）とエピソードURL（アーカイブパネル内）を使い分け |
| JSON-LD | `dateModified`・BroadcastEvent・ItemList・FAQ更新済み |
| **JSON-LD FAQ整合性** | **記事本文と一字一句一致（ユーザー修正後も再確認必須）** |
| ナビゲーションブロック | JSON-LD直前に配置（変更なしのため確認のみ） |
| **スマートクォート** | **手順16のPowerShell実行済み・「置換完了」を確認** |

---

## 表現多様化・視聴メモ（全番組共通・2026-07-16追加）

- 文言を生成・更新する全手順で `rewrite_common_rules.md` **10章（表現多様化・視聴メモ・予測の書き方）** を適用する
- 更新予告・未発表案内・CTA等の機能フレーズは、前回リライトと同じ言い回しを使い回さない（10章のバリエーション表からローテーション。同一文の記事内3回以上リピートはqa_draftがWARN検知）
- **資料③（視聴メモ）が渡された回のみ**、10章の仕様（`class="shicho-memo"`）で視聴メモブロックを挿入する。渡されない回は挿入せず、AIが感想を創作しない
- 独自予測はクッション言葉でデータと主観を区別する（10-3）
- 完了報告に「この回の独自要素」を1行含める（10-4 HCSセルフチェック）
